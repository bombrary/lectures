#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int main()
{
  int N;
  cin >> N;
  vector<int> d(N);
  for (int i = 0; i < N; i++) {
    cin >> a[i];
  }
  sort(d.begin(), d.end());

  int cnt = 0;
  for (int i = 0; i < N; i++) {
}
