#include <stdio.h>

int main(void) {
	char S[110];
	int cnt = 0;
	int i;

    scanf("%s", S);
    for(i = 0; S[i] != '\0'; i++) {
		if(S[i] == 'o') cnt++;
    }
	printf("%d\n", cnt);


    return 0;
}
